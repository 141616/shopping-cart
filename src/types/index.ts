export interface IProduct {
  availableSizes: string[];
  id: number;
  installments: number;
  isFreeShipping: boolean;
  price: number;
  style: string;
  title: string;
  front: string;
  back: string;
}
